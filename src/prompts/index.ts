import Handlebars from "handlebars";

/**
 * Prompt templates centralized. The handbook is not a recipe book — it is a
 * **learn-by-doing course** that teaches the reader to rebuild the project:
 *
 *   1. 整体浏览 → 提出"总任务"
 *   2. 把总任务拆解为一系列"子任务"（= 章节）
 *   3. 每个子任务里把要用到的技术/库/概念**当成知识点讲清楚**——按官方步骤
 *   4. 给出完整代码，跟着抄即得副本
 *   5. 每章末尾把产物**串到下一子任务**，形成完整搭建还原过程
 *
 * 这套模板保证 AI 产出的每一章既是"操作步骤"，又是"知识点小课"，
 * 还在认知层面把整本手册编织成一个递进的子任务链。
 */

export const SYSTEM_TEMPLATE = `你是一名资深工程师 + 技术作者，正在为读者编写**项目搭建实战课——一本完整的书**。读者从第一章开始照着做，最后一章合上书时，会得到一个能跑起来的完整项目副本，并在搭的过程中**掌握其中用到的每一项技术**。

整本书的承诺：
- 从第一章空目录到最后一章可发布产物，所有代码都齐全；中间不留洞。
- 每章既是"操作手册"又是"知识点小课"；既给步骤也给原理。
- 章节首尾相接：第 N 章结尾的产物等于第 N+1 章开头的前置——形成一条**完整的搭建还原过程链**。
- 不怕长。每章可以写到几千字甚至上万字，只要每一段都紧扣"读者此刻在做什么、为什么这么做、做完会得到什么"。**完整性永远优先于篇幅控制。**

教学法（贯穿全书，每一章都遵守）：
1. **总任务 → 子任务**。整本手册有一条主线：从空目录到能跑的完整副本。每章是这条主线上的一个"子任务"；写每章前先用一句话告诉读者"本章在主线上的位置、上一步交付了什么、本章要交付什么、下一步会用到它什么"。
2. **教思路，不只是给步骤**。每章必答"要解决什么具体问题？有哪些可行方案？为什么选这一种？" 把 trade-off 讲透。
3. **突出重点**。每章 2-4 个最关键的设计决策/抽象/模式，明确说"为什么这是重点"。
4. **解析难点**。每章 1-3 个最容易踩坑或反直觉的地方：朴素方案为什么不行？正确解法的关键洞察是什么？
5. **介绍技术知识点**（这是本课程的核心承诺）。每章新出现的库、工具、协议、概念，**当成知识点讲清楚**：一句话定义、设计动机、官方推荐用法（写明你引用的是哪份官方文档/规范的哪一节）、在本项目里的具体用法。**不要假设读者已经会**。
6. **按官方步骤走**。每个新工具/新库出现时，**引用其官方文档的推荐用法**——告诉读者"官方文档怎么说"+ "我们这里怎么做" + "如果有偏差，原因是什么"。绝不杜撰 API。
7. **代码必须完整**。操作步骤里出现的每个文件，都给出**完整内容**，用三个反引号代码块包裹，首行写 \`lang:相对路径\`。绝不写 "// ..." "# 省略" "truncated" 之类的占位符。读者复制完即得到与原项目逐字节一致的副本。
8. **做中学的验证**。每章末尾给出**可立即执行**的命令：跑什么、看到什么算成功、常见错误的快速排查。这是"在做中学到"的关键——错就立刻发现。
9. **提炼精髓**。每章末尾用一段话写出"跳出本项目，这一章学到的可迁移工程原则是什么"——读者明天去做别的项目能直接用上的那种。
10. **串通到下一站**。每章末尾用一两句话明确写出本章产物如何被下一章使用，形成"完整搭建还原过程"的认知串联。

写作语气：用 {{language}} 输出。直接、自信、可操作；不要套话；不要重复读者已知术语的解释，但**首次出现的术语必须解释**。`;

export const OVERVIEW_TEMPLATE = `为搭建实战课写《00 · 整体浏览与总任务》一章。这是整本课的入口，要让读者读完就拥有"我要建什么 + 怎么一步一步建"的完整心智模型。

## 项目栈速览
- 语言/运行时：{{stack.language}} / {{stack.runtime}}
- 包管理 / 构建：{{stack.packageManager}} / {{stack.buildTool}}
- 框架：{{#each stack.frameworks}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
- 测试：{{#each stack.testFrameworks}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
- 入口：{{#each stack.entryPoints}}\`{{this}}\`{{#unless @last}}, {{/unless}}{{/each}}

## 文件全景（按层级）
{{fileTree}}

## 章节地图（子任务清单，后续每章对应一个子任务）
{{chaptersMap}}

## 输出要求

# 00 · 整体浏览与总任务

## 项目意图
（这个项目要解决什么真问题？为谁解决？目标用户的痛点是什么？不要复述 README，要从代码反推出真实意图——把读者带入"作者当年面对的问题"。）

## 总任务（你接下来要重建什么）
（一段话陈述**总任务**：从空目录出发，最终交付一个能做 X 的项目。明确说总任务的**入口**是什么、**产物**是什么、**判定成功的标准**是什么。这是一切子任务的"灯塔"。）

## 整体浏览（从代码反推架构）
\`\`\`mermaid
（用 mermaid graph 画一张组件/数据流图，标出关键模块和数据走向，让读者一眼看清"东西是怎么转起来的"）
\`\`\`

## 子任务拆解（总任务怎么一步步达成）
（把总任务拆成 N 个子任务——**每个子任务对应后续一章**。用表格列出：
| 子任务编号 | 子任务标题 | 它要交付什么 | 用到的关键技术 |
| --- | --- | --- | --- |
此表是整本手册的"目录 + 心智模型"——读完此表，读者已经知道每一章他将得到什么。）

## 子任务依赖关系
\`\`\`mermaid
（画一张 mermaid 图，把上面的子任务画成节点，用箭头标明"做 A 才能做 B"。这告诉读者必须按怎样的顺序学。）
\`\`\`

## 关键设计决策（贯穿全书的"灯塔"）
（列出 3-5 条贯穿全书的核心设计决策，每条形如：**决策 X**：选择 A 而不是 B，因为 ⋯⋯。提前点出，让读者读后续每一章都能识别出这些决策的影子。）

## 贯穿全书的关键技术清单（你会一路掌握的）
（列出整本手册会反复出现的技术/库/工具，每条一行：**技术名** — 一句话作用 — 官方文档来源/规范名。例如：
- **{{stack.buildTool}}** — 把 TypeScript 源码打成可分发的产物 — 官方文档：https://...
- **{{stack.packageManager}}** — 管理依赖 + 注册 bin 命令 — 官方文档：https://...
让读者明白他读完这本书会"顺手学会"哪些可迁移技能。）

## 重难点地图
（列出后续章节中最值得重点突破的 3-5 个子任务，并简述各自的难点——告诉读者哪几章要慢读。）

## 学习路线（做中学的节奏）
（按子任务顺序，每个子任务一行：**子任务 N**：完成后你能独立做 X / 理解 Y——具体能力，不要"了解"。）

## 准备工作
（动手前必须装好的工具，每个写明版本要求和官方下载/安装说明的来源链接。）

## 怎么用这本手册
（明确告诉读者：按顺序读、每章末尾的命令必跑、跑通了再进入下一章。建立"做中学"的纪律。）

直接输出 markdown，不要前言。`;

export const CHAPTER_TEMPLATE = `为搭建实战课写《{{chapter.id}} · {{chapter.title}}》。这是总任务的第 {{chapter.id}} 个子任务，必须既是"操作手册"又是"知识点小课"，并且必须**串到下一子任务**。

## 上下文
- 项目主语言：{{stack.language}}（{{stack.runtime}}）
- 包管理器：{{stack.packageManager}}
- 本章类型：{{chapter.kind}}
{{#if chapter.focus}}
- **本章聚焦**：{{chapter.focus}}（**严格限定**：不要扩散到其他文件，也不要给其他文件的完整代码块——其他文件留给它们各自的章节。）
{{/if}}
{{#if hasDeps}}
- 前置子任务：{{#each chapter.deps}}《{{this}}》{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}

## 本章涉及的源文件（必须**完整**呈现以下每个文件的内容；不要省略，不要写占位符）
{{#each filesContent}}
### \`{{this.path}}\`
\`\`\`{{this.lang}}
{{this.content}}
\`\`\`

{{/each}}

## 输出要求（必须包含以下所有小节，缺一不可）

# {{chapter.id}} · {{chapter.title}}

## 子任务定位
（一段话：本章是总任务的第几个子任务？{{#if hasDeps}}它接在哪些子任务之后（上游产物是什么）{{else}}（这是动手第一站，无上游）{{/if}}？它要交付什么具体产物？下一章会怎么用到这些产物？让读者读完这一段就能在心智地图里精准定位。）

## 目标
（本章读完，读者能独立**做出**什么、**理解**什么——具体能力，不要写"了解"。）

## 前置
{{#if hasDeps}}
（提醒读者已完成的前置子任务，以及读者现在手上应该已经有的产物。列出可验证的清单——例如"现在你应该已经能跑 \`npm run typecheck\` 通过"。）
{{else}}
（无前置时写"无；这是动手第一站，只需要装好准备工作里列的工具。"）
{{/if}}

## 设计思路
（本子任务要解决的具体问题是什么？有哪些可选方案？最终为什么选这一种？把 trade-off 讲透——这是全章最值得花字数的地方之一。）

## 知识点介绍（本章新出现的技术/库/概念）
（这是本课程的核心承诺：**首次出现的技术必须当成知识点讲清楚**。每个知识点用 \`### 知识点 N · 名称\` 起小节，每个小节包含：
1. **它是什么**：一句话定义（直白，不绕弯）。
2. **设计动机**：它为什么存在？解决了什么前人方案的痛点？
3. **官方怎么说**：引用官方文档/规范的具体说法，写明出处链接或文档章节名；引用官方推荐的最简典型用法（一小段代码即可）。
4. **本项目怎么用**：在本项目里我们怎么用它？与官方推荐用法有差异吗？为什么？

如果本章没有引入任何全新技术（罕见），写"本章不引入新知识点，仅复用前置章节已经介绍过的：A、B、C"——然后简短回顾每个的核心要点。**绝不省略此小节**。）

## 重点（2-4 个）
（每个重点用 \`### 重点 N · 标题\` 起小节。明确说"为什么这是重点"——它驱动了哪些设计、影响了哪些后续子任务。）

## 难点（1-3 个）
（每个难点用 \`### 难点 N · 标题\` 起小节。讲清：直觉解法是什么？为什么直觉解法不行？正确解法的关键洞察是什么？）

## 实现步骤（按官方步骤一步一步做）
（按依赖顺序逐文件给出完整代码。对每个文件：
1. 先一段"为什么是这些代码"——把它对应到上面的设计思路/重点。
2. 如果这一步引用了某个工具的官方步骤，写明"官方推荐做法是 X，我们这里就遵循 X"或"官方推荐 X，本项目因 Y 微调为 Z"。
3. 然后放完整代码块，**首行格式为** \`\`\`lang:相对路径，例如：
\`\`\`ts:src/foo.ts
（完整内容）
\`\`\`

绝不省略，绝不占位。每个文件用 \`### 文件：\\\`相对路径\\\`\` 起小节。）

## 做中学验证（跑通了再进入下一子任务）
（给出立即可执行的命令清单：
- 跑什么（具体命令）
- 看到什么算成功（具体输出/状态）
- 常见错误 → 快速排查思路（列出 2-3 个最容易踩到的报错和对应的 fix）

强调：**跑通这些命令是"学会本子任务"的判定标准**。跑不通就不要进入下一章，错误会累积成谜团。）

## 精髓（可迁移到别处的工程原则）
（一段话：跳出本项目，本章可迁移的工程原则是什么？读者明天去写别的项目能用上什么？把它写得像一条"工程箴言"。）

## 串通下一站
（一两句话：本章产物如何流入下一章？下一章会用到这里建好的什么东西？这是把整本手册编织成"完整搭建还原过程"的关键缝合线。如果本章是最后一章，写"这是终点站——你现在拥有一个完整可跑的项目副本"。）

直接输出 markdown，不要前言。`;

export const MODULE_OVERVIEW_TEMPLATE = `为搭建实战课写《{{chapter.id}} · {{chapter.title}}》。这是 **模块总览** 章——读者读完此章会拿到这个模块的"地图"：它在系统里负责什么、内部分几块、文件之间怎么协作。**不要复制每个文件的完整代码**——文件代码留给后续每文件深挖章节。

## 上下文
- 项目主语言：{{stack.language}}（{{stack.runtime}}）
- 模块名：{{chapter.module}}
- 本章类型：module-overview（**禁止**输出完整源代码块，最多每文件 ≤ 15 行的关键片段示意）
{{#if hasDeps}}
- 前置子任务：{{#each chapter.deps}}《{{this}}》{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}

## 本模块包含的文件（你需要根据它们的内容写"地图"，但**不要**整篇粘贴）
{{#each filesContent}}
### \`{{this.path}}\`
\`\`\`{{this.lang}}
{{this.contentPreview}}
\`\`\`

{{/each}}

## 输出要求（必须包含以下所有小节，缺一不可）

# {{chapter.id}} · {{chapter.title}}

## 子任务定位
（一段话：本模块在整本手册主线上的位置——它接在哪些前置子任务之后？它要交付什么？后续模块或测试章节会怎么用到它？）

## 模块职责
（用一段话清楚定义这个模块负责什么、不负责什么——边界感越锋利越好。然后用一张表格列出此模块的**对外契约**：导出的关键函数/类/类型 → 一句话作用。）

## 内部结构
\`\`\`mermaid
（用 mermaid 画一张模块**内部**的组件/调用关系图：节点是文件或关键导出，边是调用/依赖。让读者一眼看清"这个模块内部是怎么转起来的"。）
\`\`\`

紧接着用一段话解读这张图——哪里是入口、哪里是内部细节、数据/控制流怎么走。

## 文件清单与阅读顺序
（按推荐学习顺序列出本模块的所有文件，每个文件一行表格 / 列表项，格式：
- **文件路径** — 一句话角色描述 — 为什么先读它

把"先读哪个、后读哪个、为什么"讲清楚——这就是后续每文件深挖章节的顺序锚。）

## 阅读顺序说明
（紧接清单后，用 2-3 段话进一步解释依赖梯度：为什么 A 在 B 之前？哪些文件可以平行读？哪些文件改起来会牵动其他模块？）

## 关键设计决策（贯穿本模块）
（列出 2-4 条本模块层面的设计决策——不是单文件层面的实现细节，而是"模块作为整体为什么这样切分"。每条说清"做了什么 / 为什么不是别的方案 / 影响了哪些文件"。）

## 常见误读 / 易踩坑
（1-3 个新人读这个模块时容易误解的点，例如"看似可以直接调 X，实际应该走 Y"。）

## 精髓（模块层面的可迁移工程原则）
（一段话：跳出本项目，这个模块的切分方式/抽象选型有哪些可以搬到别的项目里去？把它写得像一条"工程箴言"。）

## 串通下一站
（一两句话明确：读完本章后，读者下一步应该进入哪一个或哪几个"模块·文件 深挖"子任务。给出具体子任务编号，形成清晰的串联。）

直接输出 markdown，不要前言。`;

let registered = false;
function register() {
  if (registered) return;
  Handlebars.registerHelper("eq", (a: unknown, b: unknown) => a === b);
  registered = true;
}

export function renderSystem(language: "zh" | "en"): string {
  register();
  return Handlebars.compile(SYSTEM_TEMPLATE)({
    language: language === "zh" ? "中文" : "English",
  });
}

export function renderOverview(ctx: Record<string, unknown>): string {
  register();
  return Handlebars.compile(OVERVIEW_TEMPLATE)(ctx);
}

export function renderChapter(ctx: Record<string, unknown>): string {
  register();
  return Handlebars.compile(CHAPTER_TEMPLATE)({
    ...ctx,
    hasDeps:
      Array.isArray((ctx as { chapter?: { deps?: unknown[] } }).chapter?.deps) &&
      ((ctx as { chapter: { deps: unknown[] } }).chapter.deps.length > 0),
  });
}

export function renderModuleOverview(ctx: Record<string, unknown>): string {
  register();
  return Handlebars.compile(MODULE_OVERVIEW_TEMPLATE)({
    ...ctx,
    hasDeps:
      Array.isArray((ctx as { chapter?: { deps?: unknown[] } }).chapter?.deps) &&
      ((ctx as { chapter: { deps: unknown[] } }).chapter.deps.length > 0),
  });
}
